<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:padding="24dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_no_projects"
        android:layout_width="222dp"
        android:layout_height="108dp"
        android:layout_alignStart="@+id/recycler_view"
        android:layout_alignTop="@+id/recycler_view"
        android:layout_alignEnd="@+id/recycler_view"
        android:layout_alignBottom="@+id/recycler_view"
        android:layout_marginStart="79dp"
        android:layout_marginTop="231dp"
        android:layout_marginEnd="78dp"
        android:layout_marginBottom="248dp"
        android:text="Нет доступных проектов, нажмите кнопку"
        android:textAlignment="center"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/iv_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/tv_no_projects"
        android:layout_alignTop="@+id/tv_no_projects"
        android:layout_alignEnd="@+id/tv_no_projects"
        android:layout_alignBottom="@id/fab_add_project"
        android:layout_marginStart="0dp"
        android:layout_marginTop="128dp"
        android:layout_marginEnd="0dp"
        android:layout_marginBottom="300dp"
        android:src="@drawable/ic_add" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="377dp"
        android:layout_height="603dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_project"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:layout_marginEnd="323dp"
        android:layout_marginBottom="643dp"
        android:background="@color/colorAccent"
        android:backgroundTint="@color/colorAccent"
        android:src="@drawable/ic_add"
        android:tint="@android:color/black" />

</RelativeLayout>